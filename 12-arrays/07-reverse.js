const numbers = [1, 2, 3];
 
const  newNumbers = numbers.reverse(); 
console.log(newNumbers); // 3, 2, 1

// If i want to change it back to the orginal then you have to reverse one more time

//What if i want both orginial and reversed

const arr = ['Jane', 'John', 'Alex'];
const arr2 = new Array(arr);

console.log(arr.reverse());
console.log(arr2)

