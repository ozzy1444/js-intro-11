const arr1 = [1, 2, 3]; // creating an array with array multiple
const arr2 = new Array('Chicago', 'Miami', 'LA') // creating an array with Array constructor 
// both ways of making arrays but the first one is used more and easier

// use const more when doing arrays

console.log(arr1); // [1, 2, 3]
console.log(arr2); // ['Chicago', 'Miami', 'LA']

