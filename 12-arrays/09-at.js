const arr = ['']

// first and last elements in the array 
arr[0];
arr[arr.length - 1];



const numbers = [5, 7, 12, 15];

let first = numbers[0];
let last = numbers[numbers.length - 1]
let second = numbers[1]
let secondToLast = numbers[numbers.length - 2]

let sum = first + last + second + secondToLast

console.log(sum / 2) // need to check on this ???
