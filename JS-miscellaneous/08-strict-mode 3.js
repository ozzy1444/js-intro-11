"use strict"

i = 10;

console.log(i); // ReferenceError: i is not defined

// before adding "use strict" the code would run but now we get a refrence error
// use strict will save us for later down writing the codes